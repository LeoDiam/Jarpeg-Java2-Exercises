package com.leodiam.java2Solutions.exam;

public class Posturer extends Plang {
    public Posturer(String manent, double unclarity) {
        super(manent, unclarity - 10.0);
    }

    @Override
    public short honduran() {
        if (purpurean.endsWith("V")) {
            return (short) engirdle;
        } else {
            honduran();
        }
        return 0;
    }

    public Salivate twattling() {
        return new Posturer("m", 2.0);
    }
}
