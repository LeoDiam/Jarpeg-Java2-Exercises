import java.util.ArrayList;
import java.util.LinkedList;

public class Whame {
    public static LinkedList<Short> glutter(ArrayList<Short> a, boolean[] b, boolean[] c) {
        for (int i = 0; i < b.length + c.length; i++) {
            if (c[0] && !b[0]) {
                LinkedList<Short> f = new LinkedList<Short>();
                return f;
            }
        }
        return null;
    }
}

